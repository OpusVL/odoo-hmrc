<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record id="view_esl_declaration_wizard" model="ir.ui.view">
            <field name="name">ESL Declaration</field>
            <field name="model">account.vat.esl</field>
            <field name="arch" type="xml">
            <form string="Taxes Report">
                <label colspan="4" string="This menu produces an ESL CSV for upload to HMRC based on invoices. Select one or several periods of the fiscal year. The information required for a tax declaration is automatically generated by Odoo from invoices (or payments, in some countries)."/>
                <group string="Taxes Report" col="4">
                    <field name="chart_tax_id" widget='selection'/>
                    <field name="fiscalyear_id"/>
                    <!-- <field name="display_detail"/> -->
                    <!--- <field name="based_on"/>--> <!-- the option based_on 'payment' is probably not fully compliant with what the users understand with that term. So, currently, it's seems better to remove it from the view to avoid further problems -->
                </group>

                <group string="Periods"  col="4">
                    <field name="period_from"  domain="[('fiscalyear_id', '=', fiscalyear_id)]"/>
                    <field name="period_to" domain="[('fiscalyear_id', '=', fiscalyear_id)]"/>
                </group>
                <footer>
                    <button name="create_esl" string="Generate ESL CSV" type="object" class="oe_highlight"/>
                    or
                    <button string="Cancel" class="oe_link" special="cancel"/>
                </footer>
            </form>
            </field>
        </record>

        <record id="action_esl_declaration_wizard" model="ir.actions.act_window">
            <field name="name">Account Tax Declaration</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.vat.esl</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <menuitem
            name="ESL Declaration CSV"
            parent="account.menu_tax_report"
            action="action_esl_declaration_wizard"
            id="menu_esl_declaration_wizard"
            icon="STOCK_PRINT"/>

    </data>
</openerp>
